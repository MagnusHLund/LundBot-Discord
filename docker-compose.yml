services:
  bot:
    build:
      context: ./bot
    container_name: lundbot-discord
    restart: unless-stopped
    volumes:
      - ./bot/src/appsettings.json:/app/appsettings.json:ro
      - ./bot/src/appsettings.Production.json:/app/appsettings.Production.json:ro
      - ./bot/src/Logs:/app/src/Logs
    ports:
      - 3000:65535
    networks:
      - LundBotDiscordNetwork
    env_file:
      - .env

    profiles: ['Production']

  bot-dev:
    build:
      context: ./bot
    container_name: lundbot-discord-dev
    restart: unless-stopped
    volumes:
      - ./bot:/app
      - ./bot/src/bin:/app/src/bin
      - ./bot/src/obj:/app/src/obj
      - ./bot/src/Logs:/app/src/Logs
    ports:
      - 3000:65535
      - 5679:5678
    networks:
      - LundBotDiscordNetwork
    env_file:
      - .env

    profiles: ['Development']

networks:
  LundBotDiscordNetwork:
    driver: bridge
